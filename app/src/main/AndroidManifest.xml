<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.share.contrify.qfrat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".quizplay"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".signup_act" />
        <activity android:name=".login_signup" />
        <activity
            android:name=".ActivityQuiz"
            android:label="@string/title_activity_quiz"
            android:theme="@style/NoActionBar" /> <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".firstrun">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>